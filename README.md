# financial_project_2025

TODO
Проект по моделированию кривой процентных ставок с использованием параметрических моделей.

## 🚀 Быстрый старт

1. Установите зависимости: `pip install -r requirements.txt`
2. Откройте **`Comparison.ipynb`** - главный ноутбук с анализом и сравнением моделей
3. Для деталей смотрите `DieboldLi.ipynb` и `Bliss.ipynb`

## Ноутбуки

### 1. `Comparison.ipynb` - Сравнение моделей 🔥
**Главный ноутбук для анализа!**

Сравнение Diebold-Li и Bliss:
- Метрики качества (RMSE, R², MAE)
- Динамика во времени
- Распределения ошибок
- Визуализации
- Рекомендации по выбору модели

**Запускайте этот ноутбук первым!**

---

## Модели

### 1. Модель Bliss (1997)
**Файл:** `Bliss.ipynb`

Формула: `y(t) = β₀ + β₁ × [(1 - e^(-t/τ₁))/(t/τ₁)] + β₂ × [(1 - e^(-t/τ₂))/(t/τ₂) - e^(-t/τ₂)]`

**Параметры:** 5 (β₀, β₁, β₂, τ₁, τ₂)

**Особенности:**
- Два временных параметра для большей гибкости
- Лучше моделирует сложные формы кривой
- Оптимизация через `least_squares` (метод 'trf' - Trust Region Reflective)

### 2. Модель Diebold-Li (2006)
**Файл:** `DieboldLi.ipynb`

Формула: `y(t) = β₀ + β₁ × [(1 - e^(-t/τ))/(t/τ)] + β₂ × [(1 - e^(-t/τ))/(t/τ) - e^(-t/τ)]`

**Параметры:** 3 (β₀, β₁, β₂), τ = 2 (фиксирован)

**Особенности:**
- Упрощенная модель (3 параметра вместо 5)
- Фиксированный τ = 2 года (как в оригинальной статье)
- Лучшая интерпретируемость (level, slope, curvature)
- Оптимизация через **OLS** (Ordinary Least Squares) - аналитическое решение!
- Быстро, стабильно, без проблем сходимости
- Популярна в центральных банках

## Данные

**Файл:** `data.csv`

Кривые процентных ставок с разными сроками погашения:
- Столбцы: дата и сроки (0.25, 0.5, 0.75, 1, 2, 3, 5, 7, 10, 15, 20, 30 лет)
- Период: 2020-2024
- 1257 наблюдений

## Установка

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt

# Запустить Jupyter
jupyter notebook
```

## Результаты

После обучения моделей создаются файлы:

**Diebold-Li:**
- `diebold_li_results.csv` - параметры для каждой даты (β₀, β₁, β₂, метрики)
- `diebold_li_summary.csv` - сводная статистика

**Bliss:**
- `bliss_results.csv` - параметры для каждой даты (β₀, β₁, β₂, τ₁, τ₂, метрики)
- `bliss_summary.csv` - сводная статистика

**Сравнение:**
- `comparison_results.csv` - метрики обеих моделей для каждой даты
- `comparison_summary.csv` - итоговое сравнение

## Визуализации

Оба ноутбука включают:
- Динамика параметров модели во времени
- Метрики качества (RMSE, R², MAE)
- Примеры аппроксимированных кривых на разных датах
- Анализ остатков

## Структура проекта

```
financial_project_2025/
├── Comparison.ipynb         # 🔥 Сравнение моделей (начни отсюда!)
├── DieboldLi.ipynb          # Модель Diebold-Li
├── Bliss.ipynb              # Модель Bliss
├── data.csv                 # Исходные данные
├── diebold_li_results.csv   # Результаты Diebold-Li
├── bliss_results.csv        # Результаты Bliss
├── requirements.txt         # Зависимости
├── README.md               # Этот файл
└── venv/                   # Виртуальное окружение (не в git)
```
